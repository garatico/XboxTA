def create_gamer_achievements_tb_if_exists(conn, profile):
    print()


    #cursor = conn.cursor()
    # Drop the leaderboard table if it exists
    #cursor.execute(f"DROP TABLE IF EXISTS {profile}")

    # Drop the leaderboard table if it exists
    # cursor.execute("DROP TABLE IF EXISTS gamer_achievements")

    #cursor.close()